import { createLogger } from "@/utils/logger";

const logger = createLogger("RackService");

export const rackService = {
    async createRack(refetch: any) {

    },

    async updateRack(refetch: any) {
        
    }
}